DROP TABLE accumulo.${SCHEMA}.customer;
DROP TABLE accumulo.${SCHEMA}.lineitem;
DROP TABLE accumulo.${SCHEMA}.nation;
DROP TABLE accumulo.${SCHEMA}.orders;
DROP TABLE accumulo.${SCHEMA}.part;
DROP TABLE accumulo.${SCHEMA}.partsupp;
DROP TABLE accumulo.${SCHEMA}.region;
DROP TABLE accumulo.${SCHEMA}.supplier;
